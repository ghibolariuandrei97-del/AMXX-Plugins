#if defined _great_sage_included
  #endinput
#endif
#define _great_sage_included

/**
 * Se apeleaza cand un jucator devine Great Sage.
 */
forward user_become_sage(id);

/**
 * Verifica daca un jucator este Great Sage.
 */
native is_user_sage(id);


